URL checked: https://cgiere.github.io
When: 2026-09-21 14:39
What would have made this fail: A 200 on the homepage alone would have passed while Pages was still serving the previous commit - my first two polls did exactly that - so this check greps the served HTML for a section only the new build has, fetches every local asset the page references (style.css, app.js, resume.pdf, both woff2 fonts) and compares my local SHA against `git ls-remote origin`; it would have failed if style.css or the fonts had 404'd and shipped the site as unstyled text, if a font path had been written absolute, or if the push had not actually landed.
